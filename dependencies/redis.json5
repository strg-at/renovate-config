// its bitnami
{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  description: "dependencies: redis",
  packageRules: [
    {
      // freeze bitnami legacy
      description: "prevention: disable updates for redis",
      matchDepNames: ["bitnami/redis", "redis"],
      enabled: false,
    },
  ],
}
