{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  description: "jenkins-plugins: automerge datasource non major versions",
  packageRules: [
    {
      matchDatasources: ["jenkins-plugins"],
      automerge: true,
      matchUpdateTypes: ["minor", "patch", "digest"],
      automergeType: "pr",
    },
  ],
}
