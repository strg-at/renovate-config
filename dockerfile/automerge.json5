{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  description: "dockerfile: automerge non major versions",
  packageRules: [
    {
      matchManagers: ["dockerfile"],
      automerge: true,
      matchUpdateTypes: ["minor", "patch", "digest"],
      automergeType: "pr",
    },
  ],
}
